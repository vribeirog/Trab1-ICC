#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <time.h>

#include "utils.h"
#include "sislin.h"

int main() {
    srandom(20252);

    int n, k, maxit;
    real_t omega, epsilon;

    scanf("%d", &n);
    scanf("%d", &k);
    scanf("%lf", &omega);
    scanf("%d", &maxit);
    scanf("%lf", &epsilon);

    return 0;
}